 一、ajax工作原理:
	1、什么是ajax?
		(1)异步的javascript和xml,
	   （2）作用：通过向服务器异步发送少量数据，实现异步加载，局部刷新
	2.(1) 创建XMLHttpRequest对象
    (2) 调用xhr对象的open方法：  xhr.open("get",url,true)
    (3) xhr.send()发送请求
    (4) 监测服务器处理的状态(onreadystatechange)
    (5) 如果服务器响应完毕，xhr对象要接收数据，处理数据，显示数据。
1、AJAX = Asynchronous JavaScript and XML（异步的 JavaScript 和 XML）。
2、AJAX 是一种用于创建快速动态网页的技术。
3、AJAX 不是新的编程语言，而是一种使用现有标准的新方法。
4、通过在后台与服务器进行少量数据交换，AJAX 可以使网页实现异步更新。
这意味着可以在不重新加载整个网页的情况下，对网页的某部分进行更新。
(传统的网页（不使用 AJAX）如果需要更新内容，必需重载整个网页。)

函数封装:
function   createXmlHttpRequest(){
   var xmlHttp;
     try{    //Firefox, Opera 8.0+, Safari
           xmlHttp=new XMLHttpRequest();
    }catch (e){
           try{    //Internet Explorer
                  xmlHttp=new ActiveXObject("Msxml2.XMLHTTP");
            }catch (e){
                  try{
                          xmlHttp=new ActiveXObject("Microsoft.XMLHTTP");
                  }catch (e){}  
           }
    }
   return xmlHttp;
}

１、open方法 
	作用：创建一个新的http请求，并指定此请求的方法、URL以及验证信息(比如：用户名/密码)
	语法：xhr.open("get","test.txt",true);

2、send方法：
	作用：调用了send方法后才会发出请求
	语法：xhr. send(data);
	demo:        xhr.send(“user=”+username+”&pw=”+pw);  //以post方式传递时

	//get方式发送请求时:   xhr.send(null);
XMLHttpRequest 对象的三个重要的属性：
onreadystatechange（事件属性）	存储函数（或函数名），每当 readyState 属性改变时，就会调用该函数。
readyState属性	存有 XMLHttpRequest 的状态。从 0 到 4 发生变化。
0: 请求未初始化.  (还没有调用 open 方法)
1: 服务器连接已建立 (open 方法已被调用，但 send 方法还没有被调用)
2: 请求已接收  (send 已被调用。请求已经开始)
3: 交互中 ( 服务器正在发送响应)
4: 请求已完成，且响应已就绪  （响应发送完毕）
status属性	200: "OK"
404: 未找到页面
4、xhr.responseText 属性 :  
	（1）获取服务器响应数据。
	（2）responseText 属性返回字符串形式的响应
5.创建回调函数
xhr.onreadystatechange=function(){
  if (xhr.readyState==4 && xhr.status==200){
       //接收服务器端响应数据，并用js进行处理，显示在客户端页面
       例：document.getElementById("myDiv").innerHTML=xhr.responseText;
    }
}
6.abort()方法：停止当前请求

1.创建XMLHttpRequest对象
2.调用xhr对象的open方法(配置数据) xhr.open("get",url,true)
3.xhr.send(null);发送请求
4.检测服务器处理状(onreadystatechange)
5.如果服务器相应完毕,xhr对象要接受数据,处理数据并显示数据