示例网址：http://www.jq22.com/jquery-info18108
插件描述：不用通过数据交互那么复杂，可以简单的静态排序和筛选，适用于后台数据列表等需求

window.onload = function(){
    new TableSorter("tb1");//所有列都可排序
    new TableSorter("tb2", 0, 2 , 5, 6); //那一列可以排序
    new TableSorter("tb3").OnSorted = function(c, t){
        alert("table is sorted by " + c.innerHTML + " " + (t ? "Asc" : "Desc"));
    }
}
