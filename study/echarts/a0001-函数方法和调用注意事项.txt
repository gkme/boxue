$(function(){
	//进入页面加载第一个tab图表
	setTimeout(function(){
		函数调用。。。
	},1)

	//tab切换后加载图表
	$("ul").on("click","li",function(){
		var num = $(this).index();//获取点击元素的下标
		console.log(num);
		if(num == 0){
			setTimeout(function(){
				函数调用。。。
			},300)
		}
		if(num == 1){
			setTimeout(function(){
				函数调用。。。
			},300)
		}
	})

	//模态框中的图表数据调用
	$(".bs-example-modal-lg").on("show.bs.modal",function(e){
	    函数调用。。。
	});
})